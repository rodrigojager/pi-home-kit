# Attribution

`extensions/official-todo.ts` is copied from the official Pi Coding Agent examples (`examples/extensions/todo.ts`) from `@earendil-works/pi-coding-agent` 0.85.1 and remains subject to the upstream project's license.

All third-party packages installed by the scripts retain their own licenses. This kit does not vendor those packages; it invokes Pi/npm to obtain them from their published sources.
